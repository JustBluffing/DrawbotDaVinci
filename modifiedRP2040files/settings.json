{
  // When you open the project, CMake configuration runs automatically
  "cmake.configureOnOpen": true,

  // Set the ADD_SERVO option in the cache
  "cmake.configureSettings": {
    "ADD_SERVO": "ON"
  },

  // (Optional) Directory where the build files will be generated
  "cmake.buildDirectory": "${workspaceFolder}/build"
}
